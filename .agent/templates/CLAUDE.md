# Template Stub

This file is a marker. The canonical `CLAUDE.md` lives at the project root.
The `setup-skills.sh` script links the root version into target projects.
Do NOT duplicate content here — edit the root `CLAUDE.md` instead.
